import React from "react";
import { useNavigate  } from "react-router-dom";

function UserPage(){
  let navigate = useNavigate();
  return(
    <div id="testerFont">This will be the Users Account page</div>
  )
}

export default UserPage;